{{- if .Values.crd.enable }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.crd.keep }}
    "helm.sh/resource-policy": keep
    {{- end }}
    controller-gen.kubebuilder.io/version: v0.17.3
  name: cloudflareddeployments.cloudflare.unmango.dev
spec:
  group: cloudflare.unmango.dev
  names:
    kind: CloudflaredDeployment
    listKind: CloudflaredDeploymentList
    plural: cloudflareddeployments
    singular: cloudflareddeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudflaredDeployment is the Schema for the cloudflareddeployments
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflaredDeploymentSpec defines the desired state of CloudflaredDeployment.
            properties:
              foo:
                description: Foo is an example field of CloudflaredDeployment. Edit
                  cloudflareddeployment_types.go to remove/update
                type: string
            type: object
          status:
            description: CloudflaredDeploymentStatus defines the observed state of
              CloudflaredDeployment.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end -}}
